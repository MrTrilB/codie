// This file contains settings and configuration for the MCP tool server.
// Add additional configuration as needed for your tool integrations.

module.exports = {
  mcpServer: {
    endpoint: 'http://localhost:3333', // Default MCP tool server endpoint
    // Add more settings as needed
  }
};
