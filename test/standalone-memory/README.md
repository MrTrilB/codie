# Standalone test runner for Codie persistent memory

// Usage: npx mocha --require ts-node/register --timeout 10000 test/standalone-memory/*.test.ts
// This script is for CI and local dev, and does NOT load any VS Code or extension test files.

// No code needed here; just a placeholder for documentation and future expansion.
// All test logic is in test/standalone-memory/*.test.ts
